-Mở file code bằng jupyter notebook
-thêm các ảnh vào chung thư mục với file code trên jupyter notebook
-Nếu muốn thay hình khác để kiểm tra ta tiến hành sửa tên 
ảnh ở dòng :image = cv2.imread("tên hình ảnh cần thay đổi")
ví dụ image = cv2.imread("sample.jpg")
- Run để chạy chương trình
